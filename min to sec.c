#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sec, min = 5;

    sec = min * 60;
    printf("%d", sec);
}
//program that converts a given number of minutes (e.g., 5 minutes) into seconds and prints the result.
