#include <stdio.h>
#include <stdlib.h>

int main()
{
    double length = 5, width = 2, Perimeter;

    Perimeter = 2 * (length + width);
    printf  ("Perimeter = %.2lf", Perimeter);
    return 0;
}
//program that calculates the perimeter of a rectangle.
